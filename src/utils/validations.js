export const regexNum = /^[0-9]+/;
export const regexText = "";
export const regexTextAndNumber = /^[a-zA-Z0-9 ]+$/;
export const regexEmail = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;

export const clearText = (text) => String(text).trim();
